
<%= simple_form_for [ @cocktail, @dose ] do |f|%>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.order('name ASC'), as: :select %>
  <%= f.button :submit %>
<% end %>

<%= link_to "Back to #{@cocktail.name}", cocktail_path(@cocktail) %>
